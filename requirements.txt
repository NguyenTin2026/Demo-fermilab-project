# Loose version ranges so pip can pick wheels that match whatever Python
# version Streamlit Cloud uses (currently 3.13). torch on PyPI already runs
# on CPU, so no special index is needed.
streamlit>=1.36
torch>=2.4
transformers>=4.46
accelerate>=0.30
peft>=0.11
pandas>=2.0
numpy>=1.26
scikit-learn>=1.3

# Optional export / upload features (enabled if present):
fpdf2>=2.7
python-docx>=1.1
pypdf>=4.0
