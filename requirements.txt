# Lean deps so the app fits Streamlit Community Cloud's memory limit.
# No local LLM is loaded on Cloud (a local model OOM-kills the app), so torch /
# transformers / peft / accelerate are intentionally NOT required here. Answers
# are generated via the Anthropic API instead (set ANTHROPIC_API_KEY in secrets).
streamlit>=1.36
anthropic>=0.40
pandas>=2.0
numpy>=1.26
scikit-learn>=1.3

# Optional export / upload features (enabled if present):
fpdf2>=2.7
python-docx>=1.1
pypdf>=4.0
