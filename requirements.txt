# Install the CPU-only build of PyTorch (Streamlit Cloud has no GPU).
# This keeps the download small and the build fast.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit>=1.36
torch==2.4.1
transformers==4.46.0
accelerate==1.1.1
peft==0.13.2
pandas>=2.0
numpy>=1.26,<2.0
scikit-learn>=1.3

# Optional export / upload features (enabled if present):
fpdf2>=2.7
python-docx>=1.1
pypdf>=4.0
